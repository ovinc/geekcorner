# The `ruff` formatter

Format specific file
```bash
ruff format file.py
```

Format whole folder
```bash
ruff format folder
```

Use the `--check` option to see what ruff will do without applying the changes, e.g.
```bash
ruff format --check file.py
```

